---@meta

--- Everything the display needs from the shared media pool and from the
--- character, behind functions so Core never learns LibSharedMedia exists.
---@class AppearanceSources
---@field Font fun(name: string): string
